---
name: Expo Publish
on:
  push:
    branches:
      - master
jobs:
  publish:
    name: Install and publish
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-latest]
    steps:
      - uses: actions/checkout@v1
      - uses: actions/setup-node@v1
        with:
          node-version: 12.x
      - uses: bycedric/expo-github-action@fix/auth-windows
        with:
          expo-version: 3.x
          expo-username: bycedric
          expo-password: ${{ secrets.EXPO_CLI_PASSWORD }}
      - run: expo --help
      - run: expo whoami
      - run: npm ci
      - run: expo publish
